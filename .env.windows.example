# Soccer Analytics Service - Windows (RTX 5070 Ti) Configuration
# Copy to .env before running: docker compose -f docker-compose.yml -f docker-compose.windows.yml up -d

# Port Configuration
API_PORT=8004
OLLAMA_PORT=11434
DB_PORT=5434
DOCS_PORT=8084

# PostgreSQL
POSTGRES_USER=soccer_analytics
POSTGRES_PASSWORD=changeme_soccer_2026
POSTGRES_DB=soccer_analytics

# Ollama VLM Model (8B fits in 16GB GDDR7)
VLM_MODEL=qwen3-vl:8b

# Processing
MAX_UPLOAD_SIZE_MB=50
EXTRACTION_TIMEOUT_SECONDS=300
