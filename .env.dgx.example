# Soccer Analytics Service - DGX Spark (Blackwell GPU) Configuration
# Copy to .env before running: docker compose -f docker-compose.yml -f docker-compose.dgx.yml up -d

# Port Configuration
API_PORT=8004
OLLAMA_PORT=11434
DB_PORT=5434
DOCS_PORT=8084
COLPALI_PORT=8005

# PostgreSQL
POSTGRES_USER=soccer_analytics
POSTGRES_PASSWORD=changeme_soccer_2026
POSTGRES_DB=soccer_analytics

# Ollama VLM Model (32B fits in 128GB unified memory)
VLM_MODEL=qwen3-vl:32b

# ColPali Visual Retrieval
COLPALI_MODEL=vidore/colqwen2-v1.0
COLPALI_TIMEOUT_SECONDS=120

# Processing
MAX_UPLOAD_SIZE_MB=50
EXTRACTION_TIMEOUT_SECONDS=600
